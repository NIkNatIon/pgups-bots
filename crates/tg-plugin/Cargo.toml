[package]
name = "tg-plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
wassel-sdk.workspace = true
bot-core = { path = "../bot-core" }
tg-api = { path = "../tg-api" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
http = "1"
