[package]
name = "vk-plugin"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
wassel-sdk.workspace = true
bot-core = { path = "../bot-core" }
vk-api = { path = "../vk-api" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
http = "1"
